#锐盟气泵控制指令说明

---

## 1. 帧格式

### 1.1 控制命令格式

    - 帧头: 0x5A
    - 指令: 1字节
    - 数据长度: 2字节 (不包含帧头，指令和数据长度)
    - 数据: N字节
    - CRC16校验: 2字节 (从指令开始到数据结束 N+4)
    - 帧尾: 0x55 0xAA



### 1.2 返回数据格式

    - 帧头: 0xA5
    - 指令: 1字节
    - 数据长度: 1字节 (不包含帧头，指令和数据长度)
    - 数据: N字节
    - CRC16校验: 2字节 (从指令开始到数据结束 N+3)
    - 帧尾: 0xAA 0x55

## 2. 指令码

    * 0x01 (读取固件版本)
    * 0x02 (流量档位调整)
    * 0x03 (休眠/唤醒)
    * 0xF0 (升级固件准备)
    * 0xF1 (升级固件数据)
    * 0xF2 (固件数据传输完成)
    * 0xF3 (升级成功)

## 3. 错误码定义

    * 0x00: SUCCESS              - 成功
    * 0x01: INVALID_CMD       - 无效命令
    * 0x02: INVALID_PARAM     - 无效参数
    * 0x03: INVALID_LENGTH    - 无效长度
    * 0x04: INVALID_CRC       - CRC校验错误
    * 0x05: FLASH_ERROR       - Flash操作错误
    * 0x06: FILE_TOO_LARGE    - 文件过大
    * 0x07: VERIFY_FAILED     - 验证失败
    * 0x08: UPDATE_FAILED     - 升级失败

## 4. 读取版本信息

发送指令帧如下：


| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5A | 0x01 | 0x00 | 0x00 | crc_h8 | crc_l8 | 0x55 | 0xAA |


返回数据格式如下：


| 帧头 | 指令 | 数据长度 | hw_ver | sw_major | sw_minor | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xA5 | 0x01 | 0x04 | 0x01 | 0x01 | 0x02 | 0x00 | crc_h8 | crc_l8 | 0xAA | 0x55 |

## 5. 流量档位调节

### 5.1 流量调节分6个档位：

* 0x01: 档位1, 大约是全速流量的50%
* 0x02: 档位2, 大约是全速流量的60%
* 0x03: 档位3, 大约是全速流量的70%
* 0x04: 档位4, 大约是全速流量的80%
* 0x05: 档位5, 大约是全速流量的90%
* 0x06: 档位6, 100%流量

### 5.2 指令内容

发送指令，以档位1为例：


| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | 档位数据 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5A | 0x02 | 0x00 | 0x01 | 0x01 | crc_h8 | crc_l8 | 0x55 | 0xAA |

返回数据内容：


| 帧头 | 指令 | 数据长度 | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xA5 | 0x02 | 0x01 | 0x00 | crc_h8 | crc_l8 | 0xAA | 0x55 |

## 6. 休眠/唤醒

### 6.1 状态标志

    * 0x00: 唤醒标志
    * 0x01: 休眠标志

### 6.2 休眠指令

发送指令，进入休眠：


| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | 休眠标志 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5A | 0x03 | 0x00 | 0x01 | 0x01 | crc_h8 | crc_l8 | 0x55 | 0xAA |

返回数据内容：


| 帧头 | 指令 | 数据长度 | 休眠标志 | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xA5 | 0x03 | 0x02 | 0x01 | 0x00 | crc_h8 | crc_l8 | 0xAA | 0x55 |

### 6.3 唤醒指令

发送0x00，芯片唤醒：


| 数据 |
|:----:|
| 0x00 |

返回数据内容：


| 帧头 | 指令 | 数据长度 | 唤醒标志 | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xA5 | 0x03 | 0x02 | 0x00 | 0x00 | crc_h8 | crc_l8 | 0xAA | 0x55 |

## 7. 固件升级

### 7.1 固件升级准备

发送指令格式：


| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | 数据1 | 数据2 | 数据3 | 数据4 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5A | 0xf0 | 0x00 | 0x04 | 固件大小（H_8bit） | 固件大小（L_8bit） | 固件CRC16（H_8bit） | 固件CRC16（L_8bit） | crc_h8 | crc_l8 | 0x55 | 0xAA |

返回数据内容格式：


| 帧头 | 指令 | 数据长度 | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xA5 | 0xf0 | 0x01 | 0x00 | crc_h8 | crc_l8 | 0xAA | 0x55 |

### 7.2 固件内容数据包传输

将升级固件分为多个数据包发送给下位机，使用序列号记录数据包个数，数据包大小固定为1024，最后一包数据不足1024以实际为准（序列号从0开始，依次增加）

发送指令格式：


| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | 数据包序列(H_8bit) | 数据包序列(L_8bit) | 数据(Nbyte) | CRC16(H_8bit) | CRC16(L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5A | 0xf1 | 0x00 | 0x02+N | 数据包序列(H_8bit) | 数据包序列(L_8bit) | 固件数据 | crc_h8 | crc_l8 | 0x55 | 0xAA |

返回数据内容格式：


| 帧头 | 指令 | 数据长度 | 数据包序列(H_8bit) | 数据包序列(L_8bit) | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xA5 | 0xf1 | 0x03 | 数据包序列(H_8bit) | 数据包序列(L_8bit) | 0x00 | crc_h8 | crc_l8 | 0xAA | 0x55 |

### 7.3 固件数据传输完成

发送指令帧如下：


| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5A | 0xf2 | 0x00 | 0x00 | crc_h8 | crc_l8 | 0x55 | 0xAA |

返回数据格式如下：


| 帧头 | 指令 | 数据长度 | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xA5 | 0xf2 | 0x01 | 0x00 | crc_h8 | crc_l8 | 0xAA | 0x55 |

### 7.4 固件升级成功

BootLoader收到数据完成标志后进行校验，写参数等，完成后返回升级成功数据包，上位机需要等待这个数据包确认升级成功。
返回数据包格式如下：


| 帧头 | 指令 | 数据长度 | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xA5 | 0xf3 | 0x01 | 0x00 | crc_h8 | crc_l8 | 0xAA | 0x55 |

## 8. CRC16
  数据包crc16和升级包crc16均使用下面crc16校验方法

  crc16计算算法如下：

    uint16_t crc16(uint8_t* buff, uint32_t len)
    {
        uint16_t crc = 0;

        while(len--)
        {
            crc ^= (uint16_t) (*(buff++)) << 8;

            for(int i = 0; i < 8; i++)
            {
                if(crc & 0x8000)
                {
                    crc = (crc << 1) ^ 0x1021;
                }
                else
                {
                    crc = crc << 1;
                }
            }
        }

        return crc;
    }
