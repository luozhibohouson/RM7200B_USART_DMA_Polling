#锐盟气泵控制指令说明

---

## 1. 帧格式

### 1.1 控制命令格式

    - 帧头: 0x5B
    - 指令: 1字节
    - 数据长度: 2字节 (不包含帧头，指令和数据长度)
    - 数据: N字节
    - CRC16校验: 2字节 (从指令开始到数据结束 N+4)
    - 帧尾: 0x55 0xAA

### 1.2 返回数据格式

    - 帧头: 0xB5
    - 指令: 1字节
    - 数据长度: 1字节 (不包含帧头，指令和数据长度)
    - 数据: N字节
    - CRC16校验: 2字节 (从指令开始到数据结束 N+3)
    - 帧尾: 0xAA 0x55

## 2. 指令码

    * 0x01 (读取固件版本)
    * 0x02 (流量档位调整/查询)
    * 0x03 (气泵历史最大电压/最大电流查询)
    * 0x04 (当前电压/电流查询)
    * 0x05 (故障上报/查询)

## 3. 错误码定义

    * 0x00: SUCCESS           - 成功
    * 0x01: INVALID_CMD       - 无效命令
    * 0x02: INVALID_PARAM     - 无效参数
    * 0x03: INVALID_LENGTH    - 无效长度
    * 0x04: INVALID_CRC       - CRC校验错误

## 4. 读取固件版本

发送指令帧如下：

| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5B | 0x01 | 0x00 | 0x00 | crc_h8 | crc_l8 | 0x55 | 0xAA |

返回数据格式如下：

| 帧头 | 指令 | 数据长度 | hw_ver | sw_major | sw_minor | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xB5 | 0x01 | 0x04 | 0x01 | 0x01 | 0x02 | 0x00 | crc_h8 | crc_l8 | 0xAA | 0x55 |

## 5. 流量档位调节和查询

### 5.1 流量调节分6个档位：

* 0x01: 档位1, 大约是全速流量的50%
* 0x02: 档位2, 大约是全速流量的60%
* 0x03: 档位3, 大约是全速流量的70%
* 0x04: 档位4, 大约是全速流量的80%
* 0x05: 档位5, 大约是全速流量的90%
* 0x06: 档位6, 100%流量
* 0xfe: 关闭输出
* 0xff: 查询指令

### 5.2 档位调节指令内容

发送指令，以档位1为例：


| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | 档位数据 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5B | 0x02 | 0x00 | 0x01 | 0x01 | crc_h8 | crc_l8 | 0x55 | 0xAA |

返回数据内容：


| 帧头 | 指令 | 数据长度 | 档位数据 | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xB5 | 0x02 | 0x02 | 0x01 | 0x00 | crc_h8 | crc_l8 | 0xAA | 0x55 |

### 5.3 档位查询指令内容

发送查询指令，以当前档位1为例：

| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | 查询指令 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5B | 0x02 | 0x00 | 0x01 | 0xff | crc_h8 | crc_l8 | 0x55 | 0xAA |

返回数据内容：

| 帧头 | 指令 | 数据长度 | 当前档位 | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xB5 | 0x02 | 0x02 | 0x01 | 0x00 | crc_h8 | crc_l8 | 0xAA | 0x55 |

## 6. 气泵历史最大电压/最大电流查询

发送指令帧如下：


| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5B | 0x03 | 0x00 | 0x00 | crc_h8 | crc_l8 | 0x55 | 0xAA |

返回数据格式如下：返回40000mV，10000uA

| 帧头 | 指令 | 数据长度 | 最大电压/mV(H_8bit) | 最大电压/mV(L_8bit) | 最大电流/uA(H_8bit) | 最大电流/uA(L_8bit) | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xB5 | 0x03 | 0x05 | 0x9C | 0x40 | 0x27 | 0x10 | crc_h8 | crc_l8 | 0xAA | 0x55 |

## 7. 当前电压/电流查询

发送指令帧如下：返回40000mV，10000uA

| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5B | 0x04 | 0x00 | 0x00 | crc_h8 | crc_l8 | 0x55 | 0xAA |

返回数据格式如下：返回40000mV，10000uA

| 帧头 | 指令 | 数据长度 | 最大电压/mV(HH_8bit) | 最大电压/mV(HL_8bit) | 最大电压/mV(LH_8bit) | 最大电压/mV(LL_8bit) | 最大电流/uA(HH_8bit) | 最大电流/uA(HL_8bit) | 最大电流/uA(LH_8bit) | 最大电流/uA(LL_8bit) |错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xB5 | 0x04 | 0x09 | 0x00 | 0x00 | 0x9C | 0x40 | 0x00 | 0x00 | 0x27 | 0x10 | crc_h8 | crc_l8 | 0xAA | 0x55 |

## 8. 上报故障代码

### 8.1 故障代码标志
    * 0x00: NORMAL            - 正常
    * 0x01: OVER_VOLTAGE      - 过压（主动上报）
    * 0x02: OVER_CURRENT      - 过流（主动上报）
    * 0x03: SETTING_FAILED    - 调档失败（主动上报）
    * 0x04: NOT_LOAD          - 空载（主动上报）
    * 0xff: QUERY             - 查询指令

### 8.2 上报故障指令内容

上报故障代码，以调档失败为例：

| 帧头 | 指令 | 数据长度 | 故障代码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xB5 | 0x05 | 0x01 | 0x03 | crc_h8 | crc_l8 | 0xAA | 0x55 |

返回数据内容：

| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5B | 0x05 | 0x00 | 0x01 | 0x00 | crc_h8 | crc_l8 | 0x55 | 0xAA |

### 8.3 查询故障代码指令内容

发送查询指令，以调档失败为例：

| 帧头 | 指令 | 数据长度(H_8bit) | 数据长度(L_8bit) | 查询指令 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0x5B | 0x05 | 0x00 | 0x01 | 0xff | crc_h8 | crc_l8 | 0x55 | 0xAA |

返回数据内容：

| 帧头 | 指令 | 数据长度 | 故障代码 | 错误码 | CRC16 (H_8bit) | CRC16 (L_8bit) | 帧尾1 | 帧尾2 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|:----:|
| 0xB5 | 0x05 | 0x02 | 0x03 | 0x00 | crc_h8 | crc_l8 | 0xAA | 0x55 |

## 9. CRC16
  数据包crc16和升级包crc16均使用下面crc16校验方法

  crc16计算算法如下：

    uint16_t crc16(uint8_t* buff, uint32_t len)
    {
        uint16_t crc = 0;

        while(len--)
        {
            crc ^= (uint16_t) (*(buff++)) << 8;

            for(int i = 0; i < 8; i++)
            {
                if(crc & 0x8000)
                {
                    crc = (crc << 1) ^ 0x1021;
                }
                else
                {
                    crc = crc << 1;
                }
            }
        }

        return crc;
    }
